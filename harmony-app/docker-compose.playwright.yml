services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.58.0-noble
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CI=true
    network_mode: host
    command: ["npx", "playwright", "test"]
